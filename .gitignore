# OS
.DS_Store
Thumbs.db
desktop.ini
.thumbnail

# Editors / IDE
.vscode/
.idea/
*.swp
*.swo

# Node (if we add tooling later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp.*
.yarn/

# Build output
dist/
build/
out/
.next/
.cache/

# Env / secrets
.env
.env.*
*.pem
*.key

# Logs
*.log
logs/

# Python bytecode cache (serve.py / gen_*.py)
__pycache__/
*.pyc

# /goal orchestrator artifacts (internal automation logs, not app code)
.goal/

# Claude Code local settings (keep personal, do not share)
.claude/settings.local.json

# Large source asset — the app ships the generated .webp strips, not this
mascot animation.mp4

# Local backup of the original full-size PNGs (replaced by optimized .webp).
# Redundant with git history, so no need to ship this ~80MB folder.
_img-backup/

# Source assets / refs — not needed at runtime, kept locally only.
# Mascot_anim: raw mascot frames (the app ships the compiled mascot-*-strip.webp).
# uploads/_fig: user screenshots & Figma references.
Mascot_anim/
uploads/
_fig/
